version: '3.8'

services:
  whatsapp-bot:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./token.json:/app/token.json
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
